document.getElementById("history").onclick = function (session)
{
    const sessionList = document.getElementById("sessions");
/*    session.forEach(request => {
        sessionList.innerHTML += (createSessionElement(session));
    });*/
}

 createSessionElement(session)
{
    var table = document.getElementById("sessions");

}
